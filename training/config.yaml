project: lyman-alpha-bubble-size
entity: null
seed: 42

data:
  input_csv: training/data/bubble_size_dataset.csv
  target_column: target_peak_radius_mpc_h
  val_fraction: 0.2

model:
  type: random_forest
  n_estimators: 500
  max_depth: 16
  min_samples_split: 2
  min_samples_leaf: 1
  n_jobs: -1

output:
  model_dir: training/artifacts
  model_name: rf_bubble_size.joblib

wandb:
  enabled: true
  run_name: null

